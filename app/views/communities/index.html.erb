<% content_for :title, "Ruby Communities & Meetups | ChooseRuby" %>
<% content_for :meta_description, "Find the most active Ruby communities, forums, and meetups to ask questions, share wins, and grow as a Rails developer." %>

<div class="space-y-16 pb-16">
  <section class="rounded-3xl bg-white p-8 shadow-sm ring-1 ring-slate-100 sm:p-12">
    <div class="mx-auto max-w-3xl text-center space-y-6">
      <h1 class="text-4xl font-semibold tracking-tight text-slate-900 sm:text-5xl">
        Connect with the Ruby community
      </h1>
      <p class="text-lg text-slate-600">
        Ask questions, pair with mentors, and stay on top of new releases in welcoming spaces for Rubyists of every level.
      </p>
      <%= link_to "Submit a community", new_resource_submission_path, class: "inline-flex items-center justify-center rounded-full bg-rose-500 px-5 py-3 text-sm font-semibold text-white shadow-sm transition hover:bg-rose-400" %>
    </div>
  </section>

  <% if @communities.any? %>
    <section class="space-y-6">
      <div class="flex flex-col gap-3 text-center">
        <h2 class="text-3xl font-semibold text-slate-900">Active spaces</h2>
        <p class="text-base text-slate-600">Join the conversations where Ruby devs plan meetups, swap patterns, and celebrate launches.</p>
      </div>

      <div class="grid gap-6 md:grid-cols-2 lg:grid-cols-3">
        <% @communities.each do |community| %>
          <article class="flex h-full flex-col justify-between rounded-3xl border border-slate-100 bg-white p-6 shadow-sm transition hover:-translate-y-1 hover:shadow-xl">
            <div class="space-y-4">
              <span class="inline-flex w-fit items-center gap-2 rounded-full bg-slate-100 px-3 py-1 text-xs font-semibold text-slate-600">
                <%= community.platform %>
                <% if community.is_official? %>
                  <span class="inline-flex items-center gap-1 text-[10px] font-semibold uppercase tracking-wide text-emerald-600">
                    <span class="h-1.5 w-1.5 rounded-full bg-emerald-500"></span>
                    Official
                  </span>
                <% end %>
              </span>
              <h3 class="text-xl font-semibold text-slate-900"><%= community.platform %> community</h3>
              <p class="text-sm text-slate-600">
                <%= community.member_count.present? ? number_with_delimiter(community.member_count) : "Growing" %> members sharing updates, answering questions, and pairing on Rails problems.
              </p>
            </div>
            <div class="mt-6">
              <%= link_to community.join_url, target: "_blank", rel: "noopener", class: "group inline-flex items-center gap-2 text-sm font-semibold text-rose-600 hover:text-rose-500" do %>
                Join now
                <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4 transition group-hover:translate-x-1" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M9 5l7 7-7 7" />
                </svg>
              <% end %>
            </div>
          </article>
        <% end %>
      </div>
    </section>
  <% else %>
    <section class="rounded-3xl border border-dashed border-slate-200 bg-slate-50 p-12 text-center text-slate-600">
      We’re mapping the best Ruby meetups and online hubs. Share your favorite space and we’ll feature it here soon.
    </section>
  <% end %>

  <section class="rounded-3xl bg-slate-900 px-6 py-14 text-white shadow-lg sm:px-10 lg:px-16">
    <div class="mx-auto grid max-w-5xl gap-10 lg:grid-cols-[1fr,0.9fr] lg:items-center">
      <div class="space-y-4">
        <h2 class="text-3xl font-semibold sm:text-4xl">Stay in the loop between meetups</h2>
        <p class="text-base text-slate-200">
          We’re working on a monthly digest covering new gems, case studies, and community highlights. Be first in line when it launches.
        </p>
      </div>
      <div class="rounded-2xl bg-white/10 p-6 backdrop-blur">
        <p class="text-sm text-rose-100">
          Newsletter sign-up coming soon.
          In the meantime, <%= mail_to "hello@chooseruby.com", "reach out", class: "font-semibold text-white underline underline-offset-4" %> if you’d like to collaborate on community spotlights.
        </p>
      </div>
    </div>
  </section>
</div>
