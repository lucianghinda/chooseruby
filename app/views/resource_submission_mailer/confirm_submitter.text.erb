Thanks for sharing a Ruby resource!
===================================

Hi <%= @submission.try(:contact_name) || @submission.try(:submitter_name) || "there" %>,

We're excited to review your submission:

- Title: <%= @submission.title %>
- URL: <%= @submission.url %>
- Type: <%= (@submission.try(:resource_type) || @submission.entryable_type).humanize %>
<% if @submission.experience_level.present? %>
- Experience level: <%= @submission.experience_level.capitalize %>
<% end %>

Our editorial team reviews every submission for quality and freshness. You can expect an update within the next few business days.

If we have any questions, we'll reach out to <%= @submission.try(:contact_email) || @submission.try(:submitter_email) %>.

In the meantime, explore the latest additions on <%= root_url %>.

— The ChooseRuby team
