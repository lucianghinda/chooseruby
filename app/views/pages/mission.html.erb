<% content_for :title, "ChooseRuby Product Mission" %>
<% content_for :meta_description, "See how ChooseRuby curates the Ruby ecosystem, who we serve, and the problems we solve for developers." %>

<div class="space-y-16 pb-16">
  <section class="rounded-3xl bg-white p-10 shadow-sm ring-1 ring-slate-100 sm:p-12">
    <div class="mx-auto flex max-w-5xl flex-col gap-8 lg:flex-row lg:items-center lg:justify-between">
      <div class="max-w-2xl space-y-4">
        <h1 class="text-4xl font-semibold tracking-tight text-slate-900 sm:text-5xl">
          Our mission is to make Ruby discovery effortless
        </h1>
        <p class="text-lg text-slate-600">
          ChooseRuby is the definitive directory that helps developers answer “How do I build this with Ruby?” and “Why choose Ruby?” with confidence.
        </p>
      </div>
      <div class="rounded-3xl bg-slate-900 p-6 text-white shadow-lg">
        <p class="text-sm leading-relaxed text-slate-100">
          We combine editorial curation with community insight so every resource is vetted, current, and mapped to real developer questions.
        </p>
      </div>
    </div>
  </section>

  <section class="space-y-10">
    <div class="space-y-3">
      <h2 class="text-3xl font-semibold text-slate-900">Why ChooseRuby exists</h2>
      <p class="text-base text-slate-600 max-w-3xl">
        Ruby’s ecosystem is rich—but fragmented. The best gems, tutorials, and playbooks are scattered across GitHub, blogs, and conference talks. We remove the noise by curating quality, organizing by developer intent, and keeping every recommendation fresh.
      </p>
    </div>

    <div class="grid gap-6 lg:grid-cols-3">
      <% @mission_points.each do |point| %>
        <div class="rounded-3xl border border-slate-100 bg-white p-6 shadow-sm">
          <h3 class="text-xl font-semibold text-slate-900"><%= point[:title] %></h3>
          <p class="mt-3 text-sm text-slate-600"><%= point[:description] %></p>
        </div>
      <% end %>
    </div>
  </section>

  <section class="space-y-8">
    <div class="space-y-3">
      <h2 class="text-3xl font-semibold text-slate-900">Who we serve</h2>
      <p class="text-base text-slate-600 max-w-3xl">
        ChooseRuby supports learners, practitioners, and community leaders. Each persona has distinct goals, and our curation keeps all of them moving forward.
      </p>
    </div>
    <div class="grid gap-6 md:grid-cols-2 lg:grid-cols-4">
      <% @personas.each do |persona| %>
        <div class="rounded-3xl border border-slate-100 bg-white p-6 text-left shadow-sm">
          <h3 class="text-lg font-semibold text-slate-900"><%= persona[:name] %></h3>
          <p class="mt-1 text-sm font-medium uppercase tracking-wide text-rose-500"><%= persona[:role] %></p>
          <p class="mt-3 text-sm text-slate-600"><%= persona[:goal] %></p>
        </div>
      <% end %>
    </div>
  </section>

  <section class="rounded-3xl bg-slate-900 px-6 py-14 text-white shadow-lg sm:px-10 lg:px-16">
    <div class="mx-auto flex max-w-5xl flex-col gap-6 lg:flex-row lg:items-center lg:justify-between">
      <div class="space-y-4">
        <h2 class="text-3xl font-semibold sm:text-4xl">Help us keep Ruby discovery thriving</h2>
        <p class="text-base text-slate-200">
          Share resources, request additions, or highlight underrepresented tools. Together we build the ecosystem guide Ruby deserves.
        </p>
      </div>
      <%= link_to "Submit a resource", new_resource_submission_path, class: "inline-flex items-center justify-center rounded-full bg-white px-6 py-3 text-sm font-semibold text-slate-900 shadow-sm transition hover:bg-slate-100" %>
    </div>
  </section>
</div>
