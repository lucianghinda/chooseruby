<% content_for :title, "Why Choose Ruby on Rails | ChooseRuby" %>
<% content_for :meta_description, "Understand why modern teams choose Ruby on Rails: rapid development, curated ecosystem, and a vibrant community." %>

<div class="space-y-16 pb-16">
  <section class="rounded-3xl bg-gradient-to-br from-rose-500 via-rose-400 to-amber-300 px-6 py-16 text-white shadow-2xl sm:px-10 lg:px-16">
    <div class="mx-auto flex max-w-5xl flex-col gap-8 lg:flex-row lg:items-center lg:justify-between">
      <div class="max-w-2xl space-y-4">
        <span class="inline-flex items-center gap-2 rounded-full bg-white/20 px-4 py-1 text-sm font-semibold ring-1 ring-white/40">
          Why Choose Ruby
        </span>
        <h1 class="text-4xl font-semibold tracking-tight sm:text-5xl">
          Rails 8 proves that productivity & polish can go hand in hand
        </h1>
        <p class="text-base text-rose-50 sm:text-lg">
          Ruby’s ergonomics plus Rails’ batteries-included approach let teams ship full-featured experiences quickly—without compromising maintainability.
        </p>
      </div>
      <div class="space-y-4 rounded-3xl bg-white/15 p-6 text-sm leading-relaxed text-rose-50 ring-1 ring-white/30 backdrop-blur">
        <p>“Rails empowers small teams to punch above their weight. ChooseRuby curates the ecosystem so you can focus on delivering value, not hunting for the right tooling.”</p>
        <p class="text-xs uppercase tracking-wide text-white/80">The ChooseRuby Team</p>
      </div>
    </div>
  </section>

  <section class="space-y-8">
    <div class="space-y-3 text-center">
      <h2 class="text-3xl font-semibold text-slate-900">What makes Rails stand out?</h2>
      <p class="text-base text-slate-600">
        Modern Rails brings speed, clarity, and community to every stage of building products.
      </p>
    </div>

    <div class="grid gap-6 lg:grid-cols-3">
      <% @pillars.each do |pillar| %>
        <div class="flex h-full flex-col gap-4 rounded-3xl border border-slate-100 bg-white p-6 shadow-sm">
          <span class="text-3xl"><%= pillar[:icon] %></span>
          <h3 class="text-xl font-semibold text-slate-900"><%= pillar[:title] %></h3>
          <p class="text-sm text-slate-600"><%= pillar[:description] %></p>
        </div>
      <% end %>
    </div>
  </section>

  <section class="space-y-8 rounded-3xl bg-white p-10 shadow-sm ring-1 ring-slate-100">
    <div class="space-y-3 text-center">
      <h2 class="text-3xl font-semibold text-slate-900">Rails 8 in three highlights</h2>
      <p class="text-base text-slate-600">Why now is a great moment to invest in the Ruby ecosystem.</p>
    </div>
    <div class="grid gap-6 lg:grid-cols-3">
      <% @modern_highlights.each do |highlight| %>
        <div class="space-y-3 rounded-3xl bg-slate-50 p-6 text-left">
          <h3 class="text-lg font-semibold text-slate-900"><%= highlight[:name] %></h3>
          <p class="text-sm text-slate-600"><%= highlight[:description] %></p>
        </div>
      <% end %>
    </div>
  </section>

  <section class="space-y-10">
    <div class="space-y-3 text-center">
      <h2 class="text-3xl font-semibold text-slate-900">Curated paths to explore next</h2>
      <p class="text-base text-slate-600">
        Use these collections to see real-world Rails capabilities in action.
      </p>
    </div>
    <div class="grid gap-6 lg:grid-cols-2">
      <% curated_collections_data.each do |collection| %>
        <div class="flex h-full flex-col justify-between rounded-3xl border border-slate-100 bg-white p-8 shadow-sm">
          <div class="space-y-4">
            <span class="text-3xl"><%= collection[:icon] %></span>
            <h3 class="text-2xl font-semibold text-slate-900"><%= collection[:title] %></h3>
            <p class="text-base text-slate-600"><%= collection[:description] %></p>
          </div>
          <div class="mt-6">
            <%= link_to collection_path(collection[:slug]), class: "group inline-flex items-center gap-2 text-sm font-semibold text-rose-600 hover:text-rose-500" do %>
              Explore this path
              <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4 transition group-hover:translate-x-1" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M9 5l7 7-7 7" />
              </svg>
            <% end %>
          </div>
        </div>
      <% end %>
    </div>
  </section>

  <section class="rounded-3xl bg-slate-900 px-6 py-14 text-white shadow-lg sm:px-10 lg:px-16">
    <div class="mx-auto flex max-w-5xl flex-col gap-6 lg:flex-row lg:items-center lg:justify-between">
      <div class="space-y-4">
        <h2 class="text-3xl font-semibold sm:text-4xl">Ready to experience the ecosystem?</h2>
        <p class="text-base text-slate-200">
          Head to the resource directory to discover gems, guides, and tools curated by Ruby experts.
        </p>
      </div>
      <%= link_to "Browse the directory", entries_path, class: "inline-flex items-center justify-center rounded-full bg-white px-6 py-3 text-sm font-semibold text-slate-900 shadow-sm transition hover:bg-slate-100" %>
    </div>
  </section>
</div>
